;; Hand-port backlog from bb.edn (ADR-2607173000).
{:compose {:kind :unknown, :doc "Compose one panel's prompt:  bb compose <panel-id>", :form "(apply p/shell \"clojure -M:datomic:render compose\" *command-line-args*)"}, :imagegen:health {:kind :unknown, :doc "Probe the image-gen server readiness", :form "(println (:out (p/sh \"curl -s http://localhost:8100/health\")))"}, :imagegen:up {:kind :unknown, :doc "Start the image-gen server on :8100 (AnimagineXL 4.0)", :form "(let [d \"../../ghosthacker/apps/image-gen\"] (println \"starting image-gen on :8100 (logs → /tmp/imagegen.log)\") (p/shell {:dir d, :out :write, :err :write, :out-file (fs/file \"/tmp/imagegen.log\")} \"./.venv/bin/python\" \"app.py\"))"}, :load {:kind :unknown, :doc "Load anchors + all storyboard panels into the datalevin world", :form "(p/shell \"clojure -M:datomic:render load\")"}, :render {:kind :unknown, :doc "Render one panel → PNG + provenance datom:  bb render <id> [out.png]", :form "(apply p/shell \"clojure -M:datomic:render render\" *command-line-args*)"}, :render-all {:kind :unknown, :doc "Batch render panels by id-prefix + record provenance:  bb render-all [prefix]", :form "(apply p/shell \"clojure -M:datomic:render render-all\" *command-line-args*)"}, :requires {:kind :unknown, :doc nil, :form "([babashka.process :as p] [babashka.fs :as fs])"}, :test {:kind :unknown, :doc "Full test suite (session + render + store, on the JVM)", :form "(p/shell \"clojure -M:datomic:test\")"}, :test:pure {:kind :complex, :doc "Fast pure session-FSM tests under bb (no JVM, no DB)", :form "(do (require 'sip.session-test) (let [{:keys [fail error]} (t/run-tests 'sip.session-test)] (System/exit (if (pos? (+ fail error)) 1 0))))"}, :world {:kind :unknown, :doc "Author the WebGPU world snapshot → public/snapshot.edn", :form "(p/shell \"clojure -M:datomic:build\")"}}
